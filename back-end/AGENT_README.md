# الوكيل الذكي باستخدام LangChain

تم تطوير النظام ليصبح وكيلاً ذكياً باستخدام LangChain بدلاً من مجرد ملخص دروس.

## الميزات الجديدة

### 1. وكيل ذكي تفاعلي
- محادثة تفاعلية مع الوثيقة المرفوعة
- إمكانية طرح أسئلة والحصول على إجابات ذكية
- البحث الدلالي في الوثيقة
- ذاكرة المحادثة (Memory) لتذكر السياق

### 2. أدوات الوكيل (Tools)
- **search_documents**: البحث في الوثيقة باستخدام البحث الدلالي
- **summarize_document**: إنشاء ملخص شامل للوثيقة
- **answer_question**: الإجابة على الأسئلة بناءً على السياق

### 3. واجهة محادثة
- واجهة محادثة حديثة تشبه ChatGPT
- بث مباشر للردود
- اقتراحات أسئلة جاهزة
- دعم Markdown في الردود

## API Endpoints الجديدة

### `/chat`
نقطة نهاية للوكيل الذكي:
```
GET /chat?session_id={session_id}&q={query}&model={model}
```

**المعاملات:**
- `session_id`: معرف الجلسة (مطلوب بعد رفع ملف)
- `q`: السؤال أو الاستعلام (مطلوب)
- `model`: النموذج المستخدم (اختياري)
- `language`: اللغة (افتراضي: العربية)

**مثال:**
```bash
curl "http://localhost:9000/chat?session_id=abc123&q=ما هي النقاط الرئيسية؟"
```

### `/chat/{session_id}` (DELETE)
مسح ذاكرة المحادثة للجلسة:
```bash
DELETE /chat/{session_id}
```

## الاستخدام

### 1. رفع ملف PDF
```bash
POST /upload
```

### 2. بدء محادثة
بعد رفع الملف، استخدم endpoint `/chat` لطرح الأسئلة:
```bash
GET /chat?session_id={session_id}&q=ما هي النقاط الرئيسية؟
```

### 3. في الواجهة الأمامية
- ارفع ملف PDF
- اضغط على زر التبديل 💬 للانتقال إلى وضع المحادثة
- اطرح أسئلة حول الوثيقة
- الوكيل سيجيب بناءً على محتوى الوثيقة

## المتطلبات

تم إضافة المكتبات التالية:
- `langchain>=0.1.0`
- `langchain-google-genai>=1.0.0`
- `langchain-community>=0.0.20`
- `sentence-transformers>=2.2.0`

## التثبيت

```bash
cd back-end
pip install -r requirements.txt
```

## البنية

```
back-end/
├── ai/
│   ├── agent.py          # الوكيل القديم (ملخص الدروس)
│   └── langchain_agent.py # الوكيل الجديد (LangChain)
├── main.py               # API endpoints
└── requirements.txt      # المتطلبات
```

## الفرق بين الوضعين

### وضع الملخص (القديم)
- `/summarize-gemini`: إنشاء ملخص شامل للوثيقة
- `/agent`: إنشاء درس تفاعلي

### وضع المحادثة (الجديد)
- `/chat`: محادثة تفاعلية مع الوكيل
- البحث في الوثيقة
- الإجابة على الأسئلة
- ذاكرة المحادثة

## ملاحظات

1. الوكيل يستخدم FAISS للبحث الدلالي عند توفر الفهرس
2. الذاكرة (Memory) محفوظة لكل جلسة
3. يمكن مسح الذاكرة باستخدام DELETE `/chat/{session_id}`
4. الوكيل يدعم البث المباشر (Streaming) للردود

## التطوير المستقبلي

- [ ] إضافة المزيد من الأدوات (Tools)
- [ ] دعم الملفات المتعددة في جلسة واحدة
- [ ] تحسين البحث الدلالي
- [ ] إضافة تصدير المحادثة
- [ ] دعم الصور في الوثائق

